version: 2.1

description: >
  A demonstration to write orbs in rust.

  This orb works by embedding pre-compiled (and stripped) rust binaries
  into the orb yaml itself, and does not need further network calls to setup.

display:
  home_url: "https://github.com/bencord0/rusty-orb"
  source_url: "https://github.com/bencord0/rusty-orb"
