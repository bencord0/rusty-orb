description: >
  This command echos "Hello World" using file inclusion.

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"

steps:
  - run:
      name: Hello Greeting
      shell: /usr/local/cargo/bin/hello
      command: ""
      environment:
        PARAM_TO: <<parameters.to>>
