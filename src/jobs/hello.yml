description: >
  Send a greeting, using rust

executor: default

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"

steps:
  - hello:
      to: << parameters.to >>
