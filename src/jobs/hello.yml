description: >
  Send a greeting, using rust

docker:
  - image: circleci/circleci-cli:latest

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"

steps:
  - hello:
      to: << parameters.to >>
